{
  "name": "@git-insight/mcp",
  "version": "0.1.0",
  "description": "MCP server exposing git-insight branch graph, merge preview, batch survey and merge-order planning",
  "type": "module",
  "bin": {
    "git-insight-mcp": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "node scripts/bundle.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "prepublishOnly": "pnpm run check && pnpm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "git",
    "merge-tree",
    "conflict"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@git-insight/core": "workspace:*",
    "@types/node": "^22.15.21",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3"
  }
}
