# Changelog

本文件随 VSIX 发布到 Open VSX / Cursor 扩展市场，用于展示版本变更记录。

| 版本     | 日期 | 变更项 |
|--------|------|--------|
| 0.3.1  | 2026-08-15 | 1. **合并矩阵多选**：下拉面板勾选多条分支，一次确认加入<br>2. **fillSuggested 覆盖式**：去掉 6 条上限，二次点击不累加<br>3. **into/from 选择隔离**：切模式不再串台<br>4. **MR 候选人缓存**：同仓库同渠道 5 分钟内复用；支持 `skipCandidates` 跳过<br>5. **工具栏瘦身**：移除打开/浏览按钮，Fetch 文字按钮保留，刷新分支图/跑矩阵下放到视图区<br>6. **稳定性修复**：stopUiServer 真实关闭、push 失败清临时分支、clone 失败清残留、merge-tree stderr 兜底、platform 精准匹配、chain 循环 guard |
| 0.3.0  | 2026-08-13 | 1. **合并矩阵**：一次预演多条分支 × 多个目标的所有组合，一屏看清谁合得进去、撞在哪些文件<br>2. **矩阵当待办清单用**：从逐条解决冲突到逐条申请 MR 一路点到底，每格状态（冲突 / 已处理 / 已提 MR）实时可见；处理过没有以 git 里的实际分支为准，重开面板也不会丢<br>3. **合入顺序建议**：多条分支合进同一目标时推演最省事的次序，并指出从哪一条起需要人工。全程在 git 对象库内模拟，不切分支、不改工作区<br>4. **冲突预警常驻**（默认关闭）：后台定期检查你的分支还能不能干净合入，只在变糟时提醒<br>5. **机械冲突自动解决**：`.gitignore` 这类纯行集合文件两边各加了几行时自动取并集，不再占用人工选边<br>6. **MCP server**：新增 `@git-insight/mcp`，把分支图 / 预演 / 矩阵 / 顺序开放给任意 MCP 宿主，默认只读<br>7. **CLI**：新增 `survey`（批量矩阵）与 `merge-order`（顺序推演）<br>8. **修复**：解决完冲突或创建 MR 之后，面板按钮全部变灰点不动 |
| 0.2.1  | 2026-08-13 | 1. **界面改版**：分支图 / 合并预演 / 冲突三栏 / 配置页与弹层统一到一套视觉语言<br>2. **深浅双主题**：跟随 Cursor 主题自动切换，切换即时生效，不必重开面板<br>3. **修复**：分支图长分支名画到节点框外；冲突三栏表头与代码列错位（改为滚动吸顶） |
| 0.2.0  | 2026-08-12 | 1. **AI 选边回传更可靠**：Agent 改为直接写回结果文件，不再依赖终端命令；**需使用 Agent 模式**（已移除手动粘贴回传）<br>2. **分批不再前功尽弃**：某一批失败只把该批标为待定，其余批次的裁决全部保留；单批等待上限延长到 20 分钟<br>3. **合并预演提速**：冲突正文与溯源改为并行；关联 PR 查询改为按需开启（CLI 加 `--pr`）<br>4. **安全加固**：报告渲染过滤不安全 HTML；AI 回传接口加鉴权并限本机访问；冲突临时文件改用私有目录<br>5. **修复**：中文提交信息与冲突正文乱码；非英文环境下部分 git 结果解析失败；打开面板偶发不自动预演 |
| 0.1.17 | 2026-08-08 | 1. **默认远程**：配置页可选并保存默认 remote，fetch / MR / 平台探测不再写死 `origin`<br>2. **CLI / Skill**：未传 `--remote` 时读用户配置兜底<br>3. **分支图**：节点只显示短名；本地与远程同名同 sha 合并为单节点；多 remote 分色 + 图例<br>4. **修复**：点击高亮链路被重绘清掉 |
| 0.1.16 | 2026-08-07 | 1. 冲突解决：文件级操作归位左侧「冲突文件」列，列表头 `‹ n/N ›` 切换文件<br>2. 主工具条精简为冲突块级操作：上一处 / 下一处、采用线上 / 采用我的、重置本文件 |
| 0.1.15 | 2026-08-03 | 1. **Skill / CLI 闭环**：`apply-resolve` / `prepare-mr` / `create-mr` / `open-ui`，申请 MR 三选一<br>2. 安装扩展即注册 `/git-branch-insight`，并同步到全局 skills 目录<br>3. 扩展 URI / `gitInsight.openPreview` 可种入 into·from 并自动预演 |
| 0.1.14 | 2026-08-03 | 1. 预演页与冲突三栏视觉强化；分支图图例与报告文案对齐节点配色 |
| 0.1.13 | 2026-08-03 | 1. Webview 改版：顶栏主次 CTA、步骤 Tab、状态条、合并方向侧栏<br>2. 文档补充「AI 选边（模型）」配置说明 |
| 0.1.12 | 2026-08-02 | 1. 补充 `CHANGELOG.md`，市场可展示变更记录 |
| 0.1.11 | 2026-08-02 | 1. 精简同名分支相关文案与安装说明 |
| 0.1.10 | 2026-08-02 | 1. 目标分支仅可选远程跟踪分支，源分支仍可本地或远程<br>2. 规范化后同名（如 `master` ↔ `origin/master`）不再预演 / 推临时分支 / 申请 MR<br>3. 默认目标优先 `origin/master` → `main` → `develop` |
| 0.1.9  | 2026-08-02 | 1. 调整同名分支场景下的提示文案（0.1.10 起改为不处理同名同步） |
| 0.1.8  | 2026-07 | 1. **一键申请 MR/PR**：支持 GitHub / GitLab，四种方式（本机 CLI / 扩展内 CLI / Token API / 浏览器创建页）<br>2. MR 对话框支持指派人与审核人<br>3. 一键解决冲突改为独立 worktree，不切换主工作区分支<br>4. Fetch 凭据链：本机凭据 → Token → 交互登录 |
| 0.1.0  | 2026-07 | 1. 首版：分支图、合并冲突预演、冲突选边、一键解决并推送 |
